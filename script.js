let person = {
    name: 'Sietse',
    age: 36,
    evaluations: [7, 10, 9]  
};

let colors = ['red', 'green', 'blue'];
let hello = {greeting:'Hi ik ben een object'};
colors.push('orange', 5, hello);
let last = colors[colors.length - 1];

console.log(last);